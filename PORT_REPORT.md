# Port report — `regex-lite-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `regex-lite`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 21 `use` sites from 2 other repos
- **Outbound**: 1 `use` sites to 1 other repos
- **Unresolved external**: 1 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `regex-lite` | `regex_lite` | 21 | 1 | COD-118 | 68 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (21 edges, 6 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-core` | 12 | ✅ |
| `codex-kotlin` | `codex-execpolicy-legacy` | 3 | ✅ |
| `codex-kotlin` | `codex-tui` | 2 | ✅ |
| `codex-kotlin` | `core_test_support` | 2 | ✅ |
| `codex-kotlin` | `codex-login` | 1 | ✅ |
| `wildmatch-kotlin` | `wildmatch` | 1 |  |

### Symbol-level demand (3 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `regex_lite::Regex` | 19 | 18 | 1 | 6 |
| `regex_lite::Error` | 1 | 1 | 0 | 1 |
| `regex_lite::escape` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 3 symbols)

##### `regex_lite::Regex` (19 sites, 18 codex, 1 other, 6 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `src/context_manager/history_tests.rs:31` — `use regex_lite::Regex`
- `codex-kotlin/codex-core` `tests/common/context_snapshot.rs:1` — `use regex_lite::Regex`
- `codex-kotlin/codex-core` `tests/common/lib.rs:22` — `use regex_lite::Regex`
- `codex-kotlin/codex-core` `tests/suite/abort_tasks.rs:17` — `use regex_lite::Regex`
- `codex-kotlin/codex-core` `tests/suite/agent_jobs.rs:10` — `use regex_lite::Regex`
- `codex-kotlin/codex-core` `tests/suite/approvals.rs:42` — `use regex_lite::Regex`
- `codex-kotlin/codex-core` `tests/suite/request_permissions.rs:36` — `use regex_lite::Regex`
- `codex-kotlin/codex-core` `tests/suite/request_permissions_tool.rs:35` — `use regex_lite::Regex`
- `codex-kotlin/codex-core` `tests/suite/shell_serialization.rs:21` — `use regex_lite::Regex`
- `codex-kotlin/codex-core` `tests/suite/tools.rs:38` — `use regex_lite::Regex`
- `codex-kotlin/codex-core` `tests/suite/unified_exec.rs:40` — `use regex_lite::Regex`
- `codex-kotlin/codex-execpolicy-legacy` `src/policy.rs:3` — `use regex_lite::Regex`

_… 7 more call sites._

##### `regex_lite::Error` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-execpolicy-legacy` `src/policy.rs:2` — `use regex_lite::Error as RegexError`

##### `regex_lite::escape` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-core` `tests/suite/user_shell_cmd.rs:30` — `use regex_lite::escape`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `anyhow-kotlin` | 1 | 1 | `anyhow` | COD-10 | 47 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `regex-test` | 26 | 6 | `regex-lite` |
