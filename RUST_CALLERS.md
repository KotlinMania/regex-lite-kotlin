# Rust callers — `regex-lite-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `regex-lite` | `regex_lite` | 21 | 6 | 2 | 20 |

## `regex-lite`

### Inbound symbols (3 distinct, 21 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `regex-lite`.

#### `regex_lite::Regex` — 19 site(s) (18 codex, 1 other, 6 caller crates)

- ✅ `codex-kotlin/codex-core` `src/context_manager/history_tests.rs:31` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-core` `tests/common/context_snapshot.rs:1` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-core` `tests/common/lib.rs:22` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-core` `tests/suite/abort_tasks.rs:17` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-core` `tests/suite/agent_jobs.rs:10` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-core` `tests/suite/approvals.rs:42` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-core` `tests/suite/request_permissions.rs:36` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-core` `tests/suite/request_permissions_tool.rs:35` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-core` `tests/suite/shell_serialization.rs:21` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-core` `tests/suite/tools.rs:38` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-core` `tests/suite/unified_exec.rs:40` — `use regex_lite::Regex`
- ✅ `codex-kotlin/codex-execpolicy-legacy` `src/policy.rs:3` — `use regex_lite::Regex`
- _… and 7 more site(s)._

#### `regex_lite::Error` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-execpolicy-legacy` `src/policy.rs:2` — `use regex_lite::Error as RegexError`

#### `regex_lite::escape` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-core` `tests/suite/user_shell_cmd.rs:30` — `use regex_lite::escape`
